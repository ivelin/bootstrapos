-- OS 2.8.18 idea-board admission (bet class).
-- Adapter (mcp/src/idea-board-admission.ts) is the gate: named-group kill line
-- (pay or use) admits; relationship/instrument as the primary object rejects.
-- Do not copy a keyword deny-list into SQL — that is the brittle spam this
-- rewrite replaces. No schema bump. No third clock. Implicit loop 2.8.17 stays.
-- Cos applies on supabase-pirin-ai after smell. PR agents: file lock only.
-- Do not migrate/seed/live-probe supabase-pirin-ai from a PR cloud agent.

COMMENT ON FUNCTION public.bootstrap_os_create_idea(text, text, text, boolean, text) IS
  'OS 2.8.18: MCP adapter admits by bet class (named-group kill line, pay or use). Relationship/instrument primary object is not an idea board.';
